import { Text, View } from "react-native";

const Content = ({}) => {
  return (
    <View style={{ backgroundColor: "orange" }}>
      <Text>Go Yo</Text>
    </View>
  );
};

export default Content;
